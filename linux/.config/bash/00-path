#!/bin/bash

alias ,p="$EDITOR $BASH_CONFIG/*-path"

appendpath () {
    case ":$PATH:" in
        *:"$1":*)
            ;;
        *)
            PATH="$1:$PATH"
    esac
}

appendpath "$HOME/go/bin"
appendpath "$HOME/bin"
appendpath "$HOME/.local/bin"
